{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "json-graphql-server db.js --p 3333",
    "start": "json-graphql-server db.js --port $PORT", 
    "build": "echo 'No build necessary for json-graphql-server'"
  },
  "dependencies": {
    "faker": "^5.5.3",
    "json-graphql-server": "^2.2.3"
  }
}